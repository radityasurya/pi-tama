# Changelog

## 0.1.0

- Add a two-line dashboard footer showing directory, model + thinking level, context %/window, session cost, tok/s, and git branch with changed-file count.
- Poll git status on input, tool execution, and a 3s interval.
- Coexist with the built-in header and any theme; restore the built-in footer on shutdown.
